type User {
  id: ID! @unique
  name: String!
}

type Event {
  date: DateTime
  title: String
  description: String
  href: String
  source: String
  id: ID! @unique
  read: Boolean @default(value: "false")
  indications: [Indication!]!
  categories: [Category!]!
}

type Category {
  id: ID! @unique
  name: String!
  events: [Event!]!
}

type Keyword {
  id: ID! @unique
  name: String
  description: String
  indicators: [Indicator!]!
}

type Scenario {
  id: ID! @unique
  name: String
  description: String
  indicators: [Indicator!]!
}

type Indicator {
 id: ID! @unique
 name: String
 description: String
 keywords: [Keyword!]!
 scenarios: [Scenario!]!
}

type Indication {
  id: ID! @unique
  indicator: Indicator!
  event: Event!
  strength: Strength!
  createdAt: DateTime!
  updatedAt: DateTime!
}

type Strength {
  id: ID! @unique
  strength: Int
  meaning: String
}
