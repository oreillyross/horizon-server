type User {
  id: ID! @unique
  name: String!
}

type Article {
  date: DateTime
  title: String
  description: String
  href: String
  source: String
  id: ID! @unique
  read: Boolean @default(value: "false")
}
